{
  "name": "resqmeal",
  "version": "1.0.0",
  "description": "ResQMeal - Food Waste Reduction Platform",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd resqmeal-backend && npm run dev",
    "frontend": "cd resqmeal-frontend && npm run dev",
    "build": "cd resqmeal-backend && npm install && cd ../resqmeal-frontend && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "bootstrap": "^5.3.8",
    "react-icons": "^5.5.0",
    "react-router-dom": "^7.13.0",
    "react-toastify": "^11.0.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "author": "",
  "license": "ISC"
}
